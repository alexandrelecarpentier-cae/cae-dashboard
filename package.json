{
  "name": "cae-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Dashboards CAE (client, salarié, emplacement, mission, RM, challenge) — Cloudflare Worker + assets statiques.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
