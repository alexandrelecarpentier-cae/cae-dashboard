{
  "name": "cae-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Dashboards CAE (client, salarié, emplacement, mission, RM, challenge) — Cloudflare Pages + Functions.",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=cae-dashboard"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
